"use strict";

// import * as d3 from "d3";

export default function (xAxisScale, xAxis) {
	/* create new scale ojects based on event
	const newXScale = d3.event.transform.rescaleX(xAxisScale);

	// update axes
	d3.select("g.x.axis").call(xAxis.scale(newXScale));

	// 	const xy = `${d3.event.transform.x}, ${d3.event.transform.y}`;
	const { x } = d3.event.transform;
	d3.selectAll(".stock").attr("transform", `translate(${x})`);
	*/
}
